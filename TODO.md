# model

- `Net` -- `connect(node)` -- use `ports` as a stack to build `Net`

- `Net.format`
- `Net` -- generate SVG

- `Rule`
- `ActivePair`
- `Net` has `activePairs`

# later

- `Module.defineType`

- `TermType`

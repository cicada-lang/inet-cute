# model

- `Module.defineRules`

- `ActivePair`

- `Net` has `activePairs`

- `Net.connect` build up `activePairs`

# later

- `Net` -- `render` SVG

- `Edge` -- check ports type

- `Module.defineType`

- `TermType`

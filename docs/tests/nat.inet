(define-node zero (-> [] [Nat *]))
(define-node add1 (-> [Nat] [Nat *]))
(define-node add (-> [Nat Nat *] [Nat]))

(define-rule [zero add] [])
(define-rule [add1 add] [add add1])

(define-node main (-> [Nat *] []))

(define-net two (-> [] [])
  [zero add1 zero add1 add main])

import { Ctx } from "../ctx"
import { Mod } from "../mod"
import { Word } from "../word"

export interface CutOptions {
  //
}

export function cut(
  mod: Mod,
  ctx: Ctx,
  word: Word,
  options?: CutOptions,
): void {
  //
}
